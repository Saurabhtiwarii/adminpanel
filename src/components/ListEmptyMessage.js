import React from 'react'

function ListEmptyMessage({message}) {
    return (

            <div className="noSubject"><h1 className="noSubject__msg">{message}</h1></div>
   
    )
}

export default ListEmptyMessage
